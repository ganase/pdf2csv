version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./PDF:/app/PDF
      - ./PDF_RENAMED:/app/PDF_RENAMED
      - ./CSV:/app/CSV
    restart: unless-stopped
